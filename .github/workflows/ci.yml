name: CI

on:
  push:
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Foundation check
        run: echo "CI implementation will be added in the foundation phase."
